# PostgreSQL 接続文字列（Docker Compose 構成）
DATABASE_URL=postgres://blra:blra_dev@localhost:5433/blra

# Fastify サーバー
PORT=3000
HOST=0.0.0.0

# ログレベル（trace | debug | info | warn | error | fatal）
LOG_LEVEL=info
