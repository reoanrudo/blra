node_modules/
dist/
*.log
.DS_Store
.env
.env.*
!.env.example

# web/ フロントエンドのビルド成果物とキャッシュ
web/dist/
web/.next/
web/test-results/
web/spikes/
*.bak
web/tsconfig.tsbuildinfo

# ZCode セッション状態（プロジェクト成果物ではない）
.zcode/

# spike の取得物と出力（再取得可能なため追跡しない）
spikes/out/
spikes/fixtures/raw/
spikes/node_modules/
spikes/.omc/

# 大きな調査記録は追跡するが、エディタの一時ファイルは除く
*.swp
*~

# OMC のセッション状態（プロジェクト成果物ではない）
.omc/

# hourei-rag のコードスナップショット（実装技法の参考用・git管理外）
# 設計文書は含めない。blra 設計書 v1.2 を正とする。
reference/hourei-rag/

# 取込パイプラインの原本XML（再取得可能）
data/raw/

# 法令更新パッケージ署名用の秘密鍵（再生成可能・絶対にGit管理しない）
.secrets/

# e-Gov現行版XMLの不変store（再取得可能・チェックサムで一意）
web/var/law-xml/

# brainstorm visual companion
.superpowers/

# isolated implementation worktrees
.worktrees/

# hourei-rag から移植した未使用コード群（今後段階統合で再利用）
web/src/_HoureiArchive/

# タイムスタンプ付きスクリーンショット・エビデンス画像（一時ファイル）
/[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9].jpg
/[0-9]*.jpeg
/[0-9]*.png
